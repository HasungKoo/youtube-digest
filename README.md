# youtube-digest

YouTube에서 특정 주제의 한국어 영상을 정기 수집하고 조회수 변화를 보고한다.

## 설정

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install requests python-dotenv pytest
```

`.env`에 `YOUTUBE_API_KEY`를 넣는다. 값은 저장소에 커밋하지 않는다.

## 사용

작성 예정.
