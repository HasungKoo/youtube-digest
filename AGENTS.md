# YouTube Digest — 프로젝트 규칙

YouTube에서 특정 주제의 한국어 영상을 정기 수집하고, 조회수 변화를 추적해 보고한다.

## 데이터 원칙

- 수집한 원본 JSON은 `data/raw` 아래에 보존한다. 수정하거나 삭제하지 않는다.
- 매 수집마다 타임스탬프 폴더를 만들고 manifest.json과 SHA-256을 함께 저장한다.
- 조회수 변화는 저장된 이전 수집분과 비교해 계산한다. 추정하거나 기억에 의존하지 않는다.
- 수집 시점(`collected_at_kst`)을 반드시 기록한다. 변화량 해석의 기준이 된다.

## 보고 원칙

- 영상 요약은 API가 제공한 제목과 설명에서만 만든다. 내용을 추측해 채우지 않는다.
- 영상을 보지 않았으므로 "이 영상은 좋다/나쁘다" 같은 평가를 하지 않는다.
- 한국어 판정이 애매한 영상(한글 비율 10~30%)은 버리지 말고 별도로 표시한다.
- 조회수를 보고할 때는 수집 시점을 함께 적는다.

## 안전 원칙

- YOUTUBE_API_KEY는 로그, manifest, 보고 어디에도 출력하지 않는다.
- 특정 채널이나 인물에 대한 진위 판정, 신뢰도 점수, 등급을 매기지 않는다.
- 수집 대상은 공개된 메타데이터에 한정한다.

## API 사용 원칙

- 일일 할당량 10,000 유닛을 고려한다. search는 100유닛, videos·channels 조회는 1유닛이다.
- 불필요한 재검색을 하지 않는다. 같은 결과가 필요하면 저장된 원본을 읽는다.
- API 오류는 상태 코드와 원문을 그대로 보고한다. 원인을 추측하지 않는다.

## 개발 원칙

- 정확한 반복 작업(수집, 저장, 해시, 필터, 정렬, 비교)은 Python 코드가 수행한다.
- LLM은 결과 해석과 보고문 작성만 담당한다.
- 코드 변경 후 pytest를 실행한다.
- 검색어와 기간은 하드코딩하지 않고 명령행 인자로 받는다.
